<UserControl x:Class="BingoAdmin.UI.Views.CombosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BingoAdmin.UI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <local:StringNotEmptyConverter x:Key="StringNotEmptyConverter"/>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Top Panel: Selector + Panorama -->
        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="Selecione o Bingo:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <ComboBox x:Name="BingoSelector" Width="200" DisplayMemberPath="Nome" SelectionChanged="BingoSelector_SelectionChanged"/>
                <Button Content="Atualizar Lista" Click="RefreshButton_Click" Margin="10,0,0,0" Padding="5,2"/>
            </StackPanel>

            <!-- Panorama / Filtros -->
            <StackPanel Orientation="Horizontal">
                <Button x:Name="BtnFilterDisponivel" Content="Disponíveis: 0" Click="Filter_Click" Tag="Disponivel" Margin="0,0,10,0" Padding="10,5" Background="#DDDDDD"/>
                <Button x:Name="BtnFilterPendente" Content="Pendentes: 0" Click="Filter_Click" Tag="Pendente" Margin="0,0,10,0" Padding="10,5" Background="#DDDDDD"/>
                <Button x:Name="BtnFilterConfirmado" Content="Confirmados: 0" Click="Filter_Click" Tag="Confirmado" Margin="0,0,10,0" Padding="10,5" Background="#DDDDDD"/>
                <Button x:Name="BtnFilterTotal" Content="Total: 0" Click="Filter_Click" Tag="Total" Margin="0,0,10,0" Padding="10,5" Background="#DDDDDD"/>
            </StackPanel>
        </StackPanel>

        <!-- Custom Message Area -->
        <Expander Grid.Row="1" Header="Mensagem Personalizada (WhatsApp)" Margin="0,0,0,10" IsExpanded="True">
            <TextBox x:Name="TxtMensagem" 
                     AcceptsReturn="True" 
                     Height="80" 
                     TextWrapping="Wrap" 
                     VerticalScrollBarVisibility="Auto"
                     Padding="5"
                     Text="Boa tarde pessoal, segue a lista de combos, vamos pegar os combos antes que acabem, vamos vamos vamos!!!!!"/>
        </Expander>

        <DataGrid x:Name="CombosGrid" Grid.Row="2" AutoGenerateColumns="False" CanUserAddRows="False" Margin="0,0,0,10" BeginningEdit="CombosGrid_BeginningEdit">
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Style.Triggers>
                        <!-- Linha fica cinza se tiver Dono (indicando que está salvo/preenchido) -->
                        <DataTrigger Binding="{Binding NomeDono, Converter={StaticResource StringNotEmptyConverter}}" Value="True">
                            <Setter Property="Background" Value="LightGray"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Combo #" Binding="{Binding NumeroCombo}" IsReadOnly="True" Width="80"/>
                <DataGridTextColumn Header="Dono" Binding="{Binding NomeDono}" Width="*"/>
                <DataGridComboBoxColumn Header="Status" SelectedItemBinding="{Binding Status}" Width="120">
                    <DataGridComboBoxColumn.ItemsSource>
                        <x:Array Type="{x:Type sys:String}" xmlns:sys="clr-namespace:System;assembly=mscorlib">
                            <sys:String>Disponivel</sys:String>
                            <sys:String>Reservado</sys:String>
                            <sys:String>Confirmado</sys:String>
                        </x:Array>
                    </DataGridComboBoxColumn.ItemsSource>
                </DataGridComboBoxColumn>
                <DataGridComboBoxColumn Header="Pagamento" SelectedItemBinding="{Binding Pagamento}" Width="120">
                    <DataGridComboBoxColumn.ItemsSource>
                        <x:Array Type="{x:Type sys:String}" xmlns:sys="clr-namespace:System;assembly=mscorlib">
                            <sys:String>-----</sys:String>
                            <sys:String>Pendente</sys:String>
                            <sys:String>Pago</sys:String>
                        </x:Array>
                    </DataGridComboBoxColumn.ItemsSource>
                </DataGridComboBoxColumn>
                <DataGridTemplateColumn Header="Ações" Width="200">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Salvar" Click="SaveRow_Click" Margin="2"/>
                                <Button Content="Editar" Click="EditRow_Click" Margin="2"/>
                                <Button Content="PDF" Click="GeneratePdf_Click" Margin="2"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="BtnCopy" Content="Copiar Lista Total (WhatsApp)" Click="CopyWhatsapp_Click" Padding="10,5" Background="#25D366" Foreground="White" FontWeight="Bold"/>
        </StackPanel>
    </Grid>
</UserControl>
