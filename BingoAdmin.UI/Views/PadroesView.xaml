<UserControl x:Class="BingoAdmin.UI.Views.PadroesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Lista de Padrões -->
        <DockPanel Grid.Column="0" Margin="0,0,20,0">
            <TextBlock Text="Padrões Existentes" DockPanel.Dock="Top" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
            <ListBox x:Name="PadroesList" DisplayMemberPath="Nome" SelectionChanged="PadroesList_SelectionChanged"/>
        </DockPanel>

        <!-- Editor -->
        <StackPanel Grid.Column="1">
            <TextBlock Text="Editor de Padrão" FontSize="16" FontWeight="Bold" Margin="0,0,0,20"/>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="Nome do Padrão:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBox x:Name="NomePadraoBox" Width="200"/>
            </StackPanel>

            <Border BorderBrush="Gray" BorderThickness="1" HorizontalAlignment="Left">
                <UniformGrid x:Name="GridEditor" Rows="5" Columns="5" Width="250" Height="250">
                    <!-- Buttons will be added dynamically or via style -->
                </UniformGrid>
            </Border>

            <StackPanel Orientation="Horizontal" Margin="0,20,0,0">
                <Button Content="Limpar Grid" Click="LimparGrid_Click" Margin="0,0,10,0" Padding="10,5"/>
                <Button Content="Salvar Novo Padrão" Click="SalvarPadrao_Click" Padding="10,5" Background="#007ACC" Foreground="White"/>
                <Button x:Name="BtnExcluir" Content="Excluir Selecionado" Click="ExcluirPadrao_Click" Margin="10,0,0,0" Padding="10,5" Background="#FF4444" Foreground="White" Visibility="Collapsed"/>
            </StackPanel>
            
            <TextBlock Text="Clique nas células para marcar/desmarcar as posições vencedoras." Margin="0,10,0,0" FontStyle="Italic" Foreground="Gray"/>
        </StackPanel>
    </Grid>
</UserControl>
