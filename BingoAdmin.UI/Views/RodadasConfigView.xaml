<UserControl x:Class="BingoAdmin.UI.Views.RodadasConfigView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="800">
    <Grid Margin="20">
        <Border Background="{DynamicResource CardBackgroundBrush}" CornerRadius="8" Padding="20">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" ShadowDepth="2" Opacity="0.1" Color="Black"/>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Seleção de Bingo -->
                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Selecione o Bingo:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <ComboBox x:Name="BingoSelector" Width="200" SelectionChanged="BingoSelector_SelectionChanged">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Nome}"/>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
            <Button Content="Atualizar Lista" Click="RefreshButton_Click" Margin="10,0,0,0" Padding="5,2"/>
        </StackPanel>

        <!-- Lista de Rodadas -->
        <DataGrid x:Name="RodadasGrid" Grid.Row="1" AutoGenerateColumns="False" CanUserAddRows="False" Margin="0,0,0,10" RowEditEnding="RodadasGrid_RowEditEnding">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Ordem" Binding="{Binding NumeroOrdem}" IsReadOnly="True" Width="60"/>
                <DataGridTextColumn Header="Prêmio" Binding="{Binding TipoPremio}" Width="150"/>
                <DataGridTextColumn Header="Descrição" Binding="{Binding Descricao}" Width="*"/>
                <DataGridComboBoxColumn x:Name="PadraoColumn" Header="Padrão" SelectedValueBinding="{Binding PadraoId}" DisplayMemberPath="Nome" SelectedValuePath="Id" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Botões de Ação -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
             <Button Content="Salvar Alterações" Click="SalvarTudo_Click" Padding="10,5" Margin="0,0,10,0" Style="{DynamicResource PrimaryButtonStyle}"/>
        </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
