<UserControl x:Class="BingoAdmin.UI.Views.FinanceiroView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header / Config -->
        <Border Grid.Row="0" Background="{DynamicResource CardBackgroundBrush}" Padding="15" CornerRadius="{DynamicResource ControlCornerRadius}" Effect="{DynamicResource DropShadowEffect}" Margin="0,0,0,20">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Bingo:" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="Bold"/>
                <ComboBox x:Name="BingoSelector" Width="200" SelectionChanged="BingoSelector_SelectionChanged">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Nome}"/>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>

                <TextBlock Text="Valor por Combo (R$):" VerticalAlignment="Center" Margin="30,0,10,0" FontWeight="Bold"/>
                <TextBox x:Name="TxtValorCombo" Width="100" VerticalContentAlignment="Center" 
                         Text="0,00" PreviewTextInput="TxtValor_PreviewTextInput" PreviewKeyDown="TxtValor_PreviewKeyDown"/>
                <Button Content="Salvar Valor" Click="BtnSalvarValor_Click" Margin="10,0,0,0" Padding="10,5" Style="{DynamicResource PrimaryButtonStyle}"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Background="{DynamicResource CardBackgroundBrush}" CornerRadius="{DynamicResource ControlCornerRadius}" Effect="{DynamicResource DropShadowEffect}" Padding="20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="300"/>
                </Grid.ColumnDefinitions>

            <!-- Left: Expenses Management -->
            <GroupBox Header="Gestão de Despesas e Prêmios" Margin="0,0,20,0">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10" VerticalAlignment="Bottom">
                        <StackPanel Margin="0,0,10,0">
                            <TextBlock Text="Descrição da Despesa" FontWeight="SemiBold" Margin="0,0,0,5"/>
                            <TextBox x:Name="TxtDescricaoDespesa" Width="250" Tag="Descrição" ToolTip="Descrição da Despesa"/>
                        </StackPanel>
                        
                        <StackPanel Margin="0,0,10,0">
                            <TextBlock Text="Valor (R$)" FontWeight="SemiBold" Margin="0,0,0,5"/>
                            <TextBox x:Name="TxtValorDespesa" Width="120" Tag="Valor" ToolTip="Valor (R$)"
                                     Text="0,00" PreviewTextInput="TxtValor_PreviewTextInput" PreviewKeyDown="TxtValor_PreviewKeyDown"/>
                        </StackPanel>

                        <StackPanel Margin="0,0,10,0">
                            <TextBlock Text="Tipo" FontWeight="SemiBold" Margin="0,0,0,5"/>
                            <ComboBox x:Name="CmbTipoDespesa" Width="150" SelectedIndex="0">
                                <ComboBoxItem Content="Operacional"/>
                                <ComboBoxItem Content="Premio"/>
                                <ComboBoxItem Content="Outros"/>
                            </ComboBox>
                        </StackPanel>

                        <Button Content="Adicionar" Click="BtnAddDespesa_Click" Background="#28a745" Foreground="White" Padding="15,5" VerticalAlignment="Bottom" Margin="0,0,0,1"/>
                    </StackPanel>

                    <DataGrid x:Name="GridDespesas" Grid.Row="1" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Descrição" Binding="{Binding Descricao}" Width="*"/>
                            <DataGridTextColumn Header="Tipo" Binding="{Binding Tipo}" Width="100"/>
                            <DataGridTextColumn Header="Valor" Binding="{Binding Valor, StringFormat=C2}" Width="100"/>
                            <DataGridTemplateColumn Width="80">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="Excluir" Click="BtnExcluirDespesa_Click" Background="#dc3545" Foreground="White" Padding="5,2"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </GroupBox>

            <!-- Right: Summary -->
            <GroupBox Grid.Column="1" Header="Resumo Financeiro">
                <StackPanel Margin="10">
                    <Border Background="#e9ecef" Padding="15" CornerRadius="5" Margin="0,0,0,10">
                        <StackPanel>
                            <TextBlock Text="Receita (Combos Pagos)" Foreground="#6c757d"/>
                            <TextBlock x:Name="TxtReceita" Text="R$ 0,00" FontSize="24" FontWeight="Bold" Foreground="#28a745"/>
                        </StackPanel>
                    </Border>

                    <Border Background="#e9ecef" Padding="15" CornerRadius="5" Margin="0,0,0,10">
                        <StackPanel>
                            <TextBlock Text="Total Despesas" Foreground="#6c757d"/>
                            <TextBlock x:Name="TxtDespesas" Text="R$ 0,00" FontSize="24" FontWeight="Bold" Foreground="#dc3545"/>
                        </StackPanel>
                    </Border>

                    <Border Background="{DynamicResource ExtraRoundBackgroundBrush}" Padding="15" CornerRadius="{DynamicResource ControlCornerRadius}" Margin="0,20,0,0">
                        <StackPanel>
                            <TextBlock Text="Lucro Líquido" Foreground="{DynamicResource SubTextBrush}"/>
                            <TextBlock x:Name="TxtLucro" Text="R$ 0,00" FontSize="32" FontWeight="Bold" Foreground="{DynamicResource PrimaryBrush}"/>
                        </StackPanel>
                    </Border>
                    
                    <Button Content="Atualizar Cálculos" Click="BtnAtualizar_Click" Margin="0,20,0,0" Padding="10"/>
                </StackPanel>
            </GroupBox>
            </Grid>
        </Border>
    </Grid>
</UserControl>
