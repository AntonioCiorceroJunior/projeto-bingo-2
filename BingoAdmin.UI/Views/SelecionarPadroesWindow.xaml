<Window x:Class="BingoAdmin.UI.Views.SelecionarPadroesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:BingoAdmin.UI.Views"
        Title="Selecionar Padrões" Height="500" Width="400" WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <local:MascaraToBrushCollectionConverter x:Key="MascaraToBrushConverter"/>
    </Window.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <CheckBox x:Name="ChkSelectAll" Content="Selecionar Todos" Margin="0,0,0,10" Click="ChkSelectAll_Click"/>

        <ScrollViewer Grid.Row="1">
            <ItemsControl x:Name="LstPadroes">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="LightGray" BorderThickness="1" Margin="2" Padding="5" CornerRadius="3">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                
                                <CheckBox IsChecked="{Binding IsSelected}" VerticalAlignment="Center" Margin="0,0,10,0" Click="ItemCheckBox_Click"/>
                                
                                <StackPanel Grid.Column="1" Orientation="Horizontal">
                                    <Border BorderBrush="Black" BorderThickness="1" Width="30" Height="30" Margin="0,0,10,0" Background="White">
                                        <ItemsControl ItemsSource="{Binding Mascara, Converter={StaticResource MascaraToBrushConverter}}">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <UniformGrid Rows="5" Columns="5"/>
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Rectangle Fill="{Binding}" Margin="0.5"/>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </Border>
                                    <TextBlock Text="{Binding Nome}" VerticalAlignment="Center" FontWeight="SemiBold"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="OK" Click="BtnOk_Click" Width="80" Margin="0,0,10,0"/>
            <Button Content="Cancelar" Click="BtnCancel_Click" Width="80"/>
        </StackPanel>
    </Grid>
</Window>