<Page x:Class="BingoAdmin.UI.Views.LoginView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:BingoAdmin.UI.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="LoginView">

    <Grid Background="#F0F0F0">
        <Border Background="White" CornerRadius="10" Padding="20" HorizontalAlignment="Center" VerticalAlignment="Center" Width="300">
            <StackPanel>
                <TextBlock Text="Bingo Admin" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>
                
                <StackPanel x:Name="NamePanel" Visibility="Collapsed">
                    <TextBlock Text="Nome"/>
                    <TextBox x:Name="NameBox" Margin="0,5,0,10" Padding="5"/>
                </StackPanel>

                <TextBlock Text="E-mail"/>
                <TextBox x:Name="EmailBox" Margin="0,5,0,10" Padding="5"/>
                
                <TextBlock Text="Senha"/>
                <PasswordBox x:Name="PasswordBox" Margin="0,5,0,10" Padding="5"/>
                
                <Button x:Name="ActionButton" Content="Entrar" Click="ActionButton_Click" Padding="10,5" Margin="0,10,0,0" Background="#007ACC" Foreground="White"/>
                <Button x:Name="ToggleModeButton" Content="Não tem conta? Cadastre-se" Click="ToggleModeButton_Click" Margin="0,10,0,0" Background="Transparent" BorderThickness="0" Foreground="Blue" Cursor="Hand"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>
